package Database.Cards;

public class Trap extends SpellAndTrap{
    public Trap(Icon icon, String name, String description, int cardPrice) {
        super(icon, name, description, cardPrice);
    }
}
