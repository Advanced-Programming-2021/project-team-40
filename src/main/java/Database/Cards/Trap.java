package Database.Cards;

public class Trap extends SpellAndTrap{
    public Trap(Icon icon, String name, String description, String cardNumber, int cardPrice) {
        super(icon, name, description, cardNumber, cardPrice);
    }
}
