package Database.Cards;

public class Spell extends SpellAndTrap{

    public Spell(Icon icon, String name, String description, String cardNumber, int cardPrice) {
        super(icon, name, description, cardNumber, cardPrice);
    }
}
